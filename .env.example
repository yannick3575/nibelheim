# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Site URL (required for magic link redirects in production)
# This URL must also be configured in Supabase Dashboard:
# Authentication > URL Configuration > Site URL
NEXT_PUBLIC_SITE_URL=https://your-production-domain.com

# ============================================
# Sentry Error Tracking (Optional)
# ============================================
# Get these values from https://sentry.io
# 1. Create a project in Sentry
# 2. Go to Settings > Projects > [Your Project] > Client Keys (DSN)

# Client-side DSN (visible in browser)
NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Server-side DSN (can be same as above)
SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# For source map uploads (optional, for better stack traces)
# Go to Settings > Account > Auth Tokens to create one
SENTRY_AUTH_TOKEN=your-sentry-auth-token
SENTRY_ORG=your-sentry-org
SENTRY_PROJECT=your-sentry-project

# App version for release tracking
NEXT_PUBLIC_APP_VERSION=0.1.0

# ============================================
# Google Gemini AI (for AI Inbox analysis)
# ============================================
# Get your API key from https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================
# Upstash Redis (for rate limiting)
# ============================================
# Create a free Redis database at https://console.upstash.com/
# 1. Create a new Redis database
# 2. Copy the REST API credentials from the database details page
# Note: If not configured, rate limiting will be disabled (fail-open)
UPSTASH_REDIS_REST_URL=https://your-database.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_token_here
